apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: upgrade-check
spec:
  version: v0.1.2
  homepage: https://github.com/runtimez-com/kube-upgrade-check
  shortDescription: Find what breaks before you upgrade Kubernetes
  description: |
    Reports what an upgrade to a target Kubernetes version would break in the
    cluster you are pointed at: objects still written at an API version the
    target removes, control-plane and kubelet settings it refuses to start
    with, in-tree volume plugins that stop mounting, node runtime and version
    skew, and whether add-ons such as Karpenter, ingress-nginx, cert-manager,
    Argo CD, CoreDNS and Istio support the version you are moving to.

    It reads the cluster and writes nothing. No account and no agent, and no
    data leaves the machine.

    Removed APIs are found through managed fields, so an object is caught
    whatever applied it, including server-side apply, operators and GitOps
    controllers that leave no last-applied annotation behind.

    Every check that could not run is printed with the reason and a command to
    run yourself, so a clean report cannot be one where half the checks were
    silently skipped.

    Examples:
      kubectl upgrade-check --target 1.34
      kubectl upgrade-check --target 1.34 -o json
      kubectl upgrade-check --fail-on high --strict
  caveats: |
    Some checks need permissions the built-in view role does not grant:

      * nodes/proxy, to read each node's live kubelet configuration
      * the /metrics endpoint, for one of the removed-API evidence sources

    Without them the affected checks are reported as unavailable rather than
    skipped silently. A read-only ClusterRole covering everything is in the
    repository under rbac/.
  platforms:
    - selector:
        matchLabels:
          os: darwin
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_darwin_amd64.tar.gz
      sha256: ff189367f654bf26736d541d19778aafe9bb250e3710c502e27ed5910d36d6ba
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: darwin
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_darwin_arm64.tar.gz
      sha256: de1aa1d5eb0a3c8521d14161278302bd4a2bd190743670d6ff810499fd7cb773
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_linux_amd64.tar.gz
      sha256: bc96b646de712a81e6a1c0e6e76e700d4bf9c24177af01ac66ec182300f9a277
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_linux_arm64.tar.gz
      sha256: 25eb1f7c1c12cb4e0f2c790f7112f1d0cc9b2942ce947ae049146175404ece10
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_windows_amd64.zip
      sha256: 526be3761ad733740d75342f87efe08bffac25d39eeaf5e9a4aa474dae3e6ddf
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.2/kube-upgrade-check_0.1.2_windows_arm64.zip
      sha256: 31f753bbc8e67cc1ea309ef3edb2cb6db9846230e3e16c8c6e8caac8c77cf0ef
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
