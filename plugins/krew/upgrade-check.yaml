apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: upgrade-check
spec:
  version: v0.1.1
  homepage: https://github.com/runtimez-com/kube-upgrade-check
  shortDescription: Find what breaks before you upgrade Kubernetes
  description: |
    Reports what an upgrade to a target Kubernetes version would break in the
    cluster you are pointed at: objects still written at an API version the
    target removes, control-plane and kubelet settings it refuses to start
    with, in-tree volume plugins that stop mounting, node runtime and version
    skew, and whether add-ons such as Karpenter, ingress-nginx, cert-manager,
    Argo CD, CoreDNS and Istio support the version you are moving to.

    It reads the cluster and writes nothing. No account and no agent, and no
    data leaves the machine.

    Removed APIs are found through managed fields, so an object is caught
    whatever applied it, including server-side apply, operators and GitOps
    controllers that leave no last-applied annotation behind.

    Every check that could not run is printed with the reason and a command to
    run yourself, so a clean report cannot be one where half the checks were
    silently skipped.

    Examples:
      kubectl upgrade-check --target 1.34
      kubectl upgrade-check --target 1.34 -o json
      kubectl upgrade-check --fail-on high --strict
  caveats: |
    Some checks need permissions the built-in view role does not grant:

      * nodes/proxy, to read each node's live kubelet configuration
      * the /metrics endpoint, for one of the removed-API evidence sources

    Without them the affected checks are reported as unavailable rather than
    skipped silently. A read-only ClusterRole covering everything is in the
    repository under rbac/.
  platforms:
    - selector:
        matchLabels:
          os: darwin
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_darwin_amd64.tar.gz
      sha256: 3a8f133eb047ab9493cdaea02e3213fbb33a5adecf73608ba1c2a17c041d78ed
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: darwin
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_darwin_arm64.tar.gz
      sha256: ae7bf0fe8bd4730f15693f6d1e4f1589b45b439259a165b1662e3a1821b0687a
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_linux_amd64.tar.gz
      sha256: b2ad9e8ac92dda13388bcb92dbd305688e2a72ce253f67068f880cb3eee0737e
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_linux_arm64.tar.gz
      sha256: ee22e039a511b3f3ce016c4257dfe23f8df6a95d3eecb659d7b25ee4d10ac8dc
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_windows_amd64.zip
      sha256: 9bcbe5446150388f5522c954abe47f58c4a805ace34a4b7c1e13c40dd53fcf4b
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.1/kube-upgrade-check_0.1.1_windows_arm64.zip
      sha256: f7bd34da004b510fafe551fcd90452466a3c42868a90f130821161d227663e8f
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
